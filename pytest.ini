[pytest]
testpaths = tests
# This environment has a pytest-asyncio build that is incompatible with the
# installed pytest; ResQ has no async tests, so the plugin is disabled here.
addopts = -p no:asyncio -q
