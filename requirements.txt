# ResQ — core dependencies (SIH 2026 PS26070)
# Python 3.12. Install:  pip install -r requirements.txt
fastapi==0.141.1
uvicorn==0.52.4
pandas==2.2.2
pyarrow==21.0.0
numpy==1.26.4
scikit-learn==1.5.0
scipy==1.15.2
requests==2.33.1
pdfplumber==0.11.7
matplotlib==3.10.0
shapely==2.0.7           # district_history: track-to-district distance
pytest==7.4.2            # tests/  (python -m pytest tests/)

# PyTorch is only needed to (re)train the LSTM baseline in scripts/run_models.py.
# The dashboard, forecasts and all reports work without it. Install separately if
# you want to reproduce the LSTM rows:
#   pip install torch==2.2.2 --index-url https://download.pytorch.org/whl/cpu
